# check-sum
Calculates sha256 sums between two directories you wish to compare and prints out the differences 
Input the source directory first (usually the one already on your hard drive
Input the destination directory (where you copied to or used rsync to update - it could be the same drive or on a server of your choice like the rc-drive (dougan lab). 
